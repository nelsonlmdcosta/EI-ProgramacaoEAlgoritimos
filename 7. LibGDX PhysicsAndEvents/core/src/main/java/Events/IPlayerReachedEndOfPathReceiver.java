package Events;

public interface IPlayerReachedEndOfPathReceiver
{
    // Nothing really to pass here for now, but we can always send more information through here if needed
    void OnPlayerReachedEndOfPath();
}
